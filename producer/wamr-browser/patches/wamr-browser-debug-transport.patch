diff --git a/core/iwasm/libraries/debug-engine/gdbserver.h b/core/iwasm/libraries/debug-engine/gdbserver.h
index 4560d35..2eb563c 100644
--- a/core/iwasm/libraries/debug-engine/gdbserver.h
+++ b/core/iwasm/libraries/debug-engine/gdbserver.h
@@ -6,6 +6,9 @@
 #define _GDB_SERVER_H
 
 #include "bh_platform.h"
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+#include "wasm_debug_transport.h"
+#endif
 
 #define PACKET_BUF_SIZE 0x8000
 
@@ -47,6 +50,9 @@ typedef struct WASMGDBServer {
     bool noack;
     struct WASMDebugControlThread *thread;
     rsp_recv_context_t *receive_ctx;
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+    wasm_debug_transport_t *browser_transport;
+#endif
 } WASMGDBServer;
 
 WASMGDBServer *
diff --git a/core/iwasm/libraries/debug-engine/gdbserver.c b/core/iwasm/libraries/debug-engine/gdbserver.c
index 88b2d88..6413e7a 100644
--- a/core/iwasm/libraries/debug-engine/gdbserver.c
+++ b/core/iwasm/libraries/debug-engine/gdbserver.c
@@ -37,6 +37,7 @@ wasm_create_gdbserver(const char *host, int *port)
 {
     bh_socket_t listen_fd = (bh_socket_t)-1;
     WASMGDBServer *server;
+    (void)host;
 
     bh_assert(port);
 
@@ -56,6 +57,14 @@ wasm_create_gdbserver(const char *host, int *port)
 
     memset(server->receive_ctx, 0, sizeof(rsp_recv_context_t));
 
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+    server->browser_transport = wasm_debug_transport_create();
+    if (!server->browser_transport)
+        goto fail;
+    server->noack = false;
+    *port = 1;
+    return server;
+#else
     if (0 != os_socket_create(&listen_fd, true, true)) {
         LOG_ERROR("wasm gdb server error: create socket failed");
         goto fail;
@@ -71,6 +80,7 @@ wasm_create_gdbserver(const char *host, int *port)
     server->listen_fd = listen_fd;
 
     return server;
+#endif
 
 fail:
     if (listen_fd >= 0) {
@@ -88,6 +98,9 @@ wasm_gdbserver_listen(WASMGDBServer *server)
 {
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+    return server && server->browser_transport;
+#else
     int32 ret;
 
     ret = os_socket_listen(server->listen_fd, 1);
     if (ret != 0) {
         LOG_ERROR("wasm gdb server error: socket listen failed");
@@ -102,11 +115,17 @@ fail:
     os_socket_close(server->listen_fd);
     return false;
+#endif
 }
 
 bool
 wasm_gdbserver_accept(WASMGDBServer *server)
 {
-
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+    if (!server)
+        return false;
+    server->noack = false;
+    return server->browser_transport;
+#else
     bh_socket_t sockt_fd = (bh_socket_t)-1;
 
     LOG_VERBOSE("waiting for gdb client to connect...");
@@ -128,20 +146,32 @@ fail:
     os_socket_shutdown(server->listen_fd);
     os_socket_close(server->listen_fd);
     return false;
+#endif
 }
 
 void
 wasm_gdbserver_detach(WASMGDBServer *server)
 {
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+    (void)server;
+#else
     if (server->socket_fd > 0) {
         os_socket_shutdown(server->socket_fd);
         os_socket_close(server->socket_fd);
     }
+#endif
 }
 
 void
 wasm_close_gdbserver(WASMGDBServer *server)
 {
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+    if (server && server->browser_transport) {
+        wasm_debug_transport_close(server->browser_transport);
+        server->browser_transport = NULL;
+    }
+#else
     if (server->receive_ctx) {
         wasm_runtime_free(server->receive_ctx);
+        server->receive_ctx = NULL;
     }
@@ -151,6 +179,11 @@ wasm_close_gdbserver(WASMGDBServer *server)
         os_socket_shutdown(server->listen_fd);
         os_socket_close(server->listen_fd);
     }
+#endif
+    if (server->receive_ctx) {
+        wasm_runtime_free(server->receive_ctx);
+        server->receive_ctx = NULL;
+    }
 }
 
 static inline void
@@ -294,19 +326,35 @@ wasm_gdbserver_handle_packet(WASMGDBServer *server)
     int32 n;
     char buf[1024];
 
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+    n = wasm_debug_transport_read(server->browser_transport, (uint8_t *)buf,
+                                  sizeof(buf), 1000);
+    if (n == WASM_DEBUG_TRANSPORT_INTERRUPTED)
+        return true;
+#else
     if (os_socket_settimeout(server->socket_fd, 1000) != 0) {
         LOG_ERROR("Set socket recv timeout failed");
         return false;
     }
 
     n = os_socket_recv(server->socket_fd, buf, sizeof(buf));
+#endif
 
     if (n == 0) {
+#if WASM_DEBUG_BROWSER_TRANSPORT == 0
         handle_detach_request(server, NULL);
         LOG_VERBOSE("Debugger disconnected, waiting for debugger reconnection");
+#endif
         return true;
     }
     else if (n < 0) {
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+        if (n == WASM_DEBUG_TRANSPORT_CLOSED)
+            LOG_VERBOSE("Browser debugger transport closed");
+        else
+            LOG_ERROR("Browser debugger transport failed");
+        return false;
+#else
 #if defined(BH_PLATFORM_WINDOWS)
         if (WSAGetLastError() == WSAETIMEDOUT)
 #else
@@ -316,5 +355,6 @@ wasm_gdbserver_handle_packet(WASMGDBServer *server)
             return false;
         }
+#endif
     }
     else {
         int32 i, ret;
diff --git a/core/iwasm/libraries/debug-engine/packets.c b/core/iwasm/libraries/debug-engine/packets.c
index 16bdd73..d272be1 100644
--- a/core/iwasm/libraries/debug-engine/packets.c
+++ b/core/iwasm/libraries/debug-engine/packets.c
@@ -11,9 +11,20 @@ write_data_raw(WASMGDBServer *gdbserver, const uint8 *data, ssize_t len)
 {
     ssize_t nwritten;
 
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+    nwritten = wasm_debug_transport_write_all(gdbserver->browser_transport,
+                                               data, (uint32_t)len, 1000)
+                   ? len
+                   : -1;
+#else
     nwritten = os_socket_send(gdbserver->socket_fd, data, len);
+#endif
     if (nwritten < 0) {
         LOG_ERROR("Write error\n");
-        exit(-2);
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+        return;
+#else
+        exit(-2);
+#endif
     }
 }
diff --git a/core/iwasm/libraries/debug-engine/debug_engine.c b/core/iwasm/libraries/debug-engine/debug_engine.c
--- a/core/iwasm/libraries/debug-engine/debug_engine.c
+++ b/core/iwasm/libraries/debug-engine/debug_engine.c
@@ -36,2 +36,6 @@
     os_mutex_unlock(&debug_inst->wait_lock);
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+    wasm_debug_transport_interrupt(
+        debug_inst->control_thread->server->browser_transport);
+#endif
 }
@@ -53,2 +57,6 @@
     os_mutex_unlock(&debug_inst->wait_lock);
+#if WASM_DEBUG_BROWSER_TRANSPORT != 0
+    wasm_debug_transport_interrupt(
+        debug_inst->control_thread->server->browser_transport);
+#endif
 }
